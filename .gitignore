# pixi environments
.pixi/*
!.pixi/config.toml

# local tooling environments (lint / type checkers)
.venv/
.venv-tools/

# local tooling environments (lint/type tools)
.venv/
.venv-tools/

# local references and editor state
.references/
.vscode/
.cache/
.backups/

# python caches
__pycache__/
*.py[cod]
.pytest_cache/

# runtime artifacts
face_db.npz
memory/
metrics_log.jsonl
report.txt
unknown_incidents/
custom_model_path.txt
# Sidecar lock files for cross-process advisory locking
*.lock

# local model/training artifacts
models/
runs/
*.zip
*.part
# Downloaded by `bootstrap` (Ultralytics fetches it) or produced by training.
*.pt

# frontend build artifacts
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/coverage/
frontend/.cache/

# env/secrets
.env
.env.*
!.env.example

# os noise
.DS_Store
